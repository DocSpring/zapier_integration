{
  "name": "docspring-zapier",
  "version": "1.1.0",
  "description": "DocSpring is a document generation platform for developers. Fill out and sign PDF forms, and generate PDFs from templates.",
  "main": "index.js",
  "scripts": {
    "test": "jest --testTimeout 10000",
    "validate": "zapier-platform validate",
    "push": "zapier-platform push"
  },
  "engines": {
    "node": ">=22",
    "npm": ">=5.6.0"
  },
  "dependencies": {
    "zapier-platform-core": "19.1.0"
  },
  "devDependencies": {
    "jest": "^29.6.0",
    "nock": "^13.5.4"
  },
  "private": true,
  "zapier": {
    "convertedByCLIVersion": "19.0.0"
  }
}
